//ヘッダーは契約です。ヘッダーは、それがどのように機能するかではなく、何が存在するかを示します。
//main.c には、以下の 2 つの署名を学習するためにこれが含まれており、stats.c には含まれています。
//これをインクルードすると、コンパイラーはそれらに対して実装をチェックします。
//このファイルからコードは生成されません。

int array_sum(int values[], int length);   //宣言のみ: コンパイラーは署名を学習します。
int array_max(int values[], int length);   //このヘッダーを含めた人は誰でもそれを呼び出すことができます
