//C0-S1 مثال: إخراج السلسلة وإرجاع عادي من الملف الرئيسي.
//يقوم المترجم بتخفيض هذه المساعدين إلى نفس أوامر الطباعة MIPS التي يستخدمها التجميع.
int main(void) {
  //يتم إصدار القيم الحرفية للسلسلة في مقطع البيانات مع بايت صفر زائد.
  print_string("Hello from C on webMARS!");
  //ASCII 10 هو تغذية سطر؛ print_char يصدر حرفًا واحدًا بالضبط.
  print_char(10);
  //العودة من الرئيسي تصبح خروجًا نظيفًا للبرنامج.
  return 0;
}
