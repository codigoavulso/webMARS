//C0-S1 ตัวอย่าง: เอาต์พุตสตริงและการส่งคืนปกติจาก main
//คอมไพเลอร์จะลดระดับตัวช่วยเหล่านี้ให้เป็น syscalls การพิมพ์ MIPS เดียวกับที่ใช้โดย Assembly
int main(void) {
  //ตัวอักษรสตริงจะถูกปล่อยออกมาในส่วนข้อมูลโดยมีศูนย์ไบต์ต่อท้าย
  print_string("Hello from C on webMARS!");
  //ASCII 10 คือการป้อนบรรทัด; print_char ส่งเสียงอักขระหนึ่งตัวเท่านั้น
  print_char(10);
  //การกลับจาก main จะเป็นทางออกของโปรแกรมที่สะอาด
  return 0;
}
