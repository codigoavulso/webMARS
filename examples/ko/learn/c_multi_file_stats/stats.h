//헤더는 계약입니다. 작동 방식이 아니라 존재하는 내용을 알려줍니다.
//main.c에는 아래 두 가지 서명을 학습하기 위해 포함되어 있으며 stats.c에는
//이를 포함하므로 컴파일러는 이에 대해 구현을 확인합니다.
//이 파일에서는 코드가 생성되지 않습니다.

int array_sum(int values[], int length);   //선언 전용: 컴파일러가 서명을 학습합니다.
int array_max(int values[], int length);   //이 헤더를 포함하는 사람은 누구나 이를 호출할 수 있습니다.
