#دستی برابری ٹیسٹ:
#تاخیر سے برانچنگ کے فعال ہونے کے ساتھ، اوور فلو تاخیر کی سلاٹ میں ہوتا ہے۔
#متوقع سلوک:
#- استثنائی پیغام: ریاضی کا بہاؤ
#- وجہ. بی ڈی سیٹ
#- EPC beq ہدایات کی طرف اشارہ کرتا ہے۔

.text
main:
  lui $t1, 0x7fff
  ori $t1, $t1, 0xffff
  ori $t2, $zero, 1
  beq $zero, $zero, done
  add $t0, $t1, $t2

done:
  ori $v0, $zero, 10
  syscall
