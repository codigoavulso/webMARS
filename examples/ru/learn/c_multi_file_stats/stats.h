//Заголовок — это контракт: он говорит о том, что существует, а не о том, как это работает.
//main.c включает его, чтобы изучить две подписи ниже, и stats.c
//включает его, чтобы компилятор сверил реализацию с ними.
//Из этого файла не генерируется код.

int array_sum(int values[], int length);   //только декларация: компилятор изучает подпись
int array_max(int values[], int length);   //тот, кто включает этот заголовок, может назвать его
