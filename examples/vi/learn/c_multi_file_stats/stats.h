//Tiêu đề là một hợp đồng: nó cho biết những gì tồn tại chứ không phải cách nó hoạt động.
//main.c bao gồm nó để tìm hiểu hai chữ ký bên dưới và stats.c
//bao gồm nó để trình biên dịch kiểm tra việc triển khai dựa trên chúng.
//Không có mã nào được tạo ra từ tập tin này.

int array_sum(int values[], int length);   //chỉ khai báo: trình biên dịch học chữ ký
int array_max(int values[], int length);   //bất cứ ai bao gồm tiêu đề này đều có thể gọi nó
