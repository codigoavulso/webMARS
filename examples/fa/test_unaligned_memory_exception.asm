#تست برابری دستی برای خطای آدرس در بارگذاری.
#رفتار مورد انتظار:
#- استثناء بار در 0x10010001 مطرح شد
#- آدرس بد / vaddr نشان می دهد 0x10010001

.data
value: .word 0x12345678

.text
main:
  lui $t0, 0x1001
  ori $t0, $t0, 0x0001
  lw $t1, 0($t0)
  ori $v0, $zero, 10
  syscall
