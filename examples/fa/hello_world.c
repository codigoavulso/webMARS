//C0-S1 مثال: خروجی رشته و بازگشت عادی از اصلی.
//کامپایلر این کمک‌کننده‌ها را به همان MIPS سیستم‌های چاپی استفاده شده توسط اسمبلی پایین می‌آورد.
int main(void) {
  //لفظ رشته ای در بخش داده با بایت صفر انتهایی منتشر می شود.
  print_string("Hello from C on webMARS!");
  //ASCII 10 فید خط است. print_char دقیقا یک کاراکتر منتشر می کند.
  print_char(10);
  //بازگشت از اصلی تبدیل به یک خروجی برنامه تمیز می شود.
  return 0;
}
