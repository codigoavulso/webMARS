//هدر یک قرارداد است: آنچه را که وجود دارد می گوید، نه اینکه چگونه کار می کند.
//main.c شامل آن برای یادگیری دو امضای زیر و stats.c می شود
//شامل آن می شود، بنابراین کامپایلر پیاده سازی را در برابر آنها بررسی می کند.
//هیچ کدی از این فایل تولید نمی شود.

int array_sum(int values[], int length);   //فقط اعلان: کامپایلر امضا را یاد می گیرد
int array_max(int values[], int length);   //هر کسی که این هدر را شامل می شود می تواند آن را صدا کند
